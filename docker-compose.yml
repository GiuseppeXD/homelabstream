services:
  flaresolverr:
    image: ghcr.io/flaresolverr/flaresolverr@sha256:7962759d99d7e125e108e0f5e7f3cdbcd36161776d058d1d9b7153b92ef1af9e # v3.4.6
    container_name: flaresolverr
    mem_limit: 512m
    restart: unless-stopped
    dns:
      - 1.1.1.1
      - 1.0.0.1
    environment:
      - TZ=${TZ}
      - LOG_LEVEL=info
    labels:
      dashboard.name: "FlareSolverr"
      dashboard.description: "Proxy para resolver desafios Cloudflare"
      dashboard.url: "https://flaresolverr.home.arpa"
    networks:
      - media_net
      - edge-proxy
  qbittorrent:
    image: lscr.io/linuxserver/qbittorrent@sha256:3679a75dd2304b695525d83a4ff14c458708b5da4a7ce53044240932139590e1 # 5.1.4
    container_name: qbittorrent
    mem_limit: 512m
    restart: unless-stopped
    dns:
      - 1.1.1.1
      - 1.0.0.1
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
      - WEBUI_PORT=${QBITTORRENT_WEBUI_PORT}
    labels:
      dashboard.name: "qBittorrent"
      dashboard.description: "Cliente BitTorrent"
      dashboard.url: "https://qbittorrent.home.arpa"
    ports:
      - "6881:6881/tcp"
      - "6881:6881/udp"
    volumes:
      - ./config/qbittorrent:/config
      - ./media/downloads:/downloads
    networks:
      - media_net
      - edge-proxy
  radarr:
    image: lscr.io/linuxserver/radarr@sha256:cd70546fc97169788530386b42cf47ba1b16d091b4dc2264cd54099dd13c6f7f # 6.1.1.10360-ls298
    container_name: radarr
    mem_limit: 768m
    restart: unless-stopped
    dns:
      - 1.1.1.1
      - 1.0.0.1
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    labels:
      dashboard.name: "Radarr"
      dashboard.description: "Gerenciador de filmes"
      dashboard.url: "https://radarr.home.arpa"
    volumes:
      - ./config/radarr:/config
      - ./media:/media
    networks:
      - media_net
      - edge-proxy
  sonarr:
    image: lscr.io/linuxserver/sonarr@sha256:76414c033f290d3c9f1f9dfad71150abe71d92592369a3377a5903d579e6e2b2 # 4.0.17.2952-ls305
    container_name: sonarr
    mem_limit: 768m
    restart: unless-stopped
    dns:
      - 1.1.1.1
      - 1.0.0.1
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    labels:
      dashboard.name: "Sonarr"
      dashboard.description: "Gerenciador de séries"
      dashboard.url: "https://sonarr.home.arpa"
    volumes:
      - ./config/sonarr:/config
      - ./media:/media
    networks:
      - media_net
      - edge-proxy
  prowlarr:
    image: lscr.io/linuxserver/prowlarr@sha256:9ef5d8bf832edcacb6082f9262cb36087854e78eb7b1c3e1d4375056055b2d82 # 2.3.0.5236-ls139
    container_name: prowlarr
    mem_limit: 512m
    restart: unless-stopped
    dns:
      - 1.1.1.1
      - 1.0.0.1
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    labels:
      dashboard.name: "Prowlarr"
      dashboard.description: "Gerenciador de indexadores"
      dashboard.url: "https://prowlarr.home.arpa"
    volumes:
      - ./config/prowlarr:/config
    networks:
      - media_net
      - edge-proxy
  bazarr:
    # NOTA: post-processing script configurado mas provavelmente não funciona
    # para legendas geradas pelo Whisper. O pipeline automático EN->PT-BR
    # agora é gerenciado pelo legendas-webui (watchdog + fila).
    # Deixado aqui como fallback caso seja útil no futuro.
    image: lscr.io/linuxserver/bazarr@sha256:cb57afc3bd35558e1e7062658f9d4d18a0b4c474f10afe55f0ccfd867025f24f # 1.5.6
    container_name: bazarr
    mem_limit: 512m
    restart: unless-stopped
    dns:
      - 1.1.1.1
      - 1.0.0.1
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    labels:
      dashboard.name: "Bazarr"
      dashboard.description: "Gerenciador de legendas"
      dashboard.url: "https://bazarr.home.arpa"
    volumes:
      - ./config/bazarr:/config
      - ./media:/media
    networks:
      - media_net
      - edge-proxy
  jellyfin:
    image: lscr.io/linuxserver/jellyfin@sha256:7351868a3a340049503d25da65fae19c77ce3ea58f607f702cac65b6faeec898 # 10.11.8
    container_name: jellyfin
    mem_limit: 6g
    restart: unless-stopped
    dns:
      - 1.1.1.1
      - 1.0.0.1
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    labels:
      dashboard.name: "Jellyfin"
      dashboard.description: "Servidor de mídia"
      dashboard.url: "https://jellyfin.home.arpa"
    ports:
      - "192.168.0.15:8096:8096"
    volumes:
      - ./config/jellyfin:/config
      - ./media:/media
    networks:
      - media_net
      - edge-proxy
  jellyseerr:
    image: ghcr.io/seerr-team/seerr@sha256:c4cbd5121236ac2f70a843a0b920b68a27976be57917555f1c45b08a1e6b2aad # v3.2.0
    init: true
    container_name: jellyseerr
    mem_limit: 512m
    restart: unless-stopped
    dns:
      - 1.1.1.1
      - 1.0.0.1
    environment:
      - TZ=${TZ}
      - LOG_LEVEL=info
    labels:
      dashboard.name: "Jellyseerr"
      dashboard.description: "Gerenciador de solicitações de mídia"
      dashboard.url: "https://jellyseerr.home.arpa"
    volumes:
      - ./config/jellyseerr:/app/config
    networks:
      - media_net
      - edge-proxy

  whisper-asr:
    image: onerahmet/openai-whisper-asr-webservice@sha256:03b402335881cdab2e4939b24620809df8d6ceec3bf2712217d41c8761a0f5d4 # v1.9.1
    container_name: whisper-asr
    profiles:
      - transcription
    restart: unless-stopped
    environment:
      - TZ=${TZ}
      - ASR_ENGINE=faster_whisper
      - ASR_MODEL=${WHISPER_MODEL:-large-v3}
      - ASR_DEVICE=${WHISPER_DEVICE:-cpu}
      - OMP_NUM_THREADS=${WHISPER_CPU_THREADS:-12}
    volumes:
      - ./config/whisper/cache:/root/.cache
    ports:
      - "${WHISPER_PORT}:9000"
    networks:
      - media_net
    deploy:
      resources:
        limits:
          cpus: '12.0'
          memory: ${WHISPER_MEMORY:-8G}
          pids: 200

  ollama:
    image: ollama/ollama@sha256:3ca37ec2b9cb6341b62554074205c616778fe98abcf9e4fc50361b79a07407ae # v0.22.1
    container_name: ollama
    restart: unless-stopped
    environment:
      - TZ=${TZ}
    volumes:
      - ./config/ollama:/root/.ollama
    labels:
      dashboard.name: "Ollama"
      dashboard.description: "Modelos de linguagem (IA)"
      dashboard.url: "https://ollama.home.arpa"
    networks:
      - media_net
      - edge-proxy
    deploy:
      resources:
        limits:
          cpus: '12.0'
          memory: ${OLLAMA_MEMORY:-8G}
          pids: 200

  legendas-webui:
    build:
      context: ./scripts/webui-legendas
      dockerfile: Dockerfile
    container_name: legendas-webui
    restart: unless-stopped
    user: "${PUID}:${PGID}"
    group_add:
      - "968"  # host docker group GID for /var/run/docker.sock access
    labels:
      dashboard.name: "Legendas"
      dashboard.description: "Automação de legendas com IA"
      dashboard.url: "https://legendas.home.arpa"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:rw
      - ./media:/media:rw
      - ./config/bazarr/log:/bazarr-logs:ro
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - MEDIA_DIR=/media
      - BAZARR_LOG_DIR=/bazarr-logs
      - REFRESH_INTERVAL=15
      - TRANSLATE_MODEL=${TRANSLATE_MODEL:-gemma3:4b}
      - OLLAMA_URL=http://ollama:11434/api/generate
      - BATCH_SIZE=10
      - NUM_PREDICT=2048
      - SUBTITLE_SCAN_INTERVAL=300
      - SUBTITLE_SCAN_MIN_AGE=600
      - SUBTITLE_AUTO_QUEUE_LIMIT=0
    networks:
      - media_net
      - edge-proxy
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 256M

networks:
  media_net:
    driver: bridge
  edge-proxy:
    external: true
    name: edge-proxy
